// Contact page no longer needs form handling since we removed the form
document.addEventListener('DOMContentLoaded', () => {
  // Contact page is now static - just showing email and phone
  console.log('Contact page loaded');
});